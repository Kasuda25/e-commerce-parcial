import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'e-commerce-parcial-vue',
  webDir: 'dist'
};

export default config;
